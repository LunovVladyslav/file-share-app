node_modules/
build/
dist/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

spike/lib/

# Android
android/.gradle/
android/build/
android/*/build/
android/local.properties
android/.kotlin/
*.apk

# Local working files, not part of the published project
.claude/
SPEC.md

# Signing material. Whoever holds these can publish an update that Android
# installs over this app, so they belong on the developer's machine and in CI
# secrets — never in the repository.
android/keystore.properties
*.jks
*.keystore
